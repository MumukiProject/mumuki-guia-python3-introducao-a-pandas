Muito bem! `DataFrame` é exatamente o nome que `pandas` dá para as tabelas. _E como fazemos para saber quantas linhas e colunas têm um `DataFrame`?_ :thinking_face:

Vamos descobrir no próximo exercício!
